# Firebase Auth declares peerOptional @react-native-async-storage/async-storage ^1.x while Expo/RN
# templates commonly use 2.x — npm 10 treats this as a hard conflict without legacy peer resolution.
# EAS runs `npm ci`; this file ensures cloud installs match local installs.
legacy-peer-deps=true
